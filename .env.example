# MQTT credentials are optional, but the username and password must be set together.
MQTT_USERNAME=
MQTT_PASSWORD=

# Replace both placeholders when the Weather Underground output is enabled.
WU_STATION_ID=replace-with-station-id
WU_STATION_KEY=replace-with-station-key
