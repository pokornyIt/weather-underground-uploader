services:
  weather-underground-uploader:
    image: weather-underground-uploader:local
    build:
      context: .
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    volumes:
      - ./config.yaml:/config/config.yaml:ro
